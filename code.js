'use strict';

function favIceCream(parameter) {
  var NAME_OF_ICECREAM = parameter;
  var answer = "I love " + NAME_OF_ICECREAM;
  return answer;
};

function shouting(parameter) {
  var answer = parameter.toUpperCase()
  return answer
};

function roundDown(number) {
  var answer = Math.floor(number)
  return answer
};

function theTruth() {
  return true == 1
};